functiom numberToArray(number) {

    const number = 12345;
    const numStr=String(number);

    for(leti=0;i<numStr.length;i++) {
        Array.push(Number(numStre[i]))
}

console.log(array);